---
description: quest-time-coupon web の最小制約。巨大 docs を毎回全文読まない。
alwaysApply: true
---

# quest-time-coupon / web

正本: 集約ルートの `../../AGENTS.md`。必要になった設計書だけパス指定で読む。
（本 repo の workspace ルートは `src/web/`。集約ルート `quest-time-coupon/` は `../..`）

- default branch は `develop`。PR 先も `develop`。`main` マージは CEO 確認後
- GitHub Pages は使わない（Firebase App Hosting）
- クエスト定義は `quests/`。保護者はアプリ内編集しない
- secret / `.env` / 鍵はコミットしない
- commit / push / MR は CEO 明示時のみ
- `../../docs/screen-design.md` 等はオンデマンド。Always で全文読まない
